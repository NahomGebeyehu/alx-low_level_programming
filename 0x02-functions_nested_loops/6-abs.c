#include "main.h"

/**
 * _abs - This returns the absolute value of the int
 * @n: The integer to change absolute value
 * Return: Returns Void
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
